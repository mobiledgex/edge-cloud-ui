export const styles = (theme) => ({
    btnDiv: {
        width: '100%',
        display: 'flex',
        gap: 20,
        marginTop: 20,
        justifyContent: 'right'
    },
    greenBtn: {
        backgroundColor: '#447700'
    }
});