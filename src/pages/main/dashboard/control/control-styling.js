import { makeStyles } from "@material-ui/core";

export const controlStyles = (theme => ({
    widgets: {
        height: 210,
        marginLeft: '0.1em',
        padding:5
    },
    widgets1: {
        height: 250
    }
}));