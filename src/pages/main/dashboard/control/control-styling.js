import { makeStyles } from "@material-ui/core";

export const controlStyles = (theme => ({
    widgets: {
        marginRight: '0.1em',
        height:'30%',
        padding:5
    },
    sunburst: {
        height: '70%'
    }
}));